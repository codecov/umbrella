type ComponentComparison {
    id: String!
    name: String!
    baseTotals: CoverageTotals
    headTotals: CoverageTotals
    patchTotals: CoverageTotals
    head: CoverageTotals @deprecated(reason: "Use headTotals")
    base: CoverageTotals @deprecated(reason: "Use baseTotals")
}