type ComponentComparison {
    id: String!
    name: String!
    baseTotals: CoverageTotals
    headTotals: CoverageTotals
    patchTotals: CoverageTotals
    base: CoverageTotals @deprecated(reason: "Use baseTotals instead")
    head: CoverageTotals @deprecated(reason: "Use headTotals instead")
}