input CommitsSetFilters {
  hideFailedCI: Boolean
  branchName: String
  branch: String @deprecated(reason: "Use branchName instead")
  pullId: Int
  search: String
  states: [CommitState!]
  coverageStatus: [CommitStatus!]
}
