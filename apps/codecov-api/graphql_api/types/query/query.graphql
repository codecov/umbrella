scalar DateTime

type Query {
  me: Me
  user: Me @deprecated(reason: "Use 'me' instead")
  owner(username: String!): Owner
  config: Config!
}

type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String
  endCursor: String
}
