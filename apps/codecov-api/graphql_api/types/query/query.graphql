scalar DateTime

type Query {
  me: Me
  owner(username: String!): Owner
  config: Config!
  pull(
    provider: String!
    owner: String!
    repo: String!
    pullId: Int!
  ): Pull
}

type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String
  endCursor: String
}
