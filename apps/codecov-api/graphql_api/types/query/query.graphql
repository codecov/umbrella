scalar DateTime

type Query {
  me: Me
  owner(username: String!): Owner
  config: Config!
  version: Version!
}

type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String
  endCursor: String
}
