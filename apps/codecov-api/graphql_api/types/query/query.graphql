scalar DateTime

type Query {
  me: Me
  currentUser: Me @deprecated(reason: "Use 'me' instead")
  owner(username: String!): Owner
  config: Config!
}

type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String
  endCursor: String
}
