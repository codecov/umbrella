scalar DateTime

type Query {
  me: Me
  owner(username: String!): Owner
  account(username: String!): Account
  config: Config!
}

type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String
  endCursor: String
}
