scalar DateTime

type Query {
  me: Me
  owner(username: String, name: String): Owner
  config: Config!
}

type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String
  endCursor: String
}
