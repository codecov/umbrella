
type CoverageTotals {
  percentCovered: Float
  fileCount: Int
  lineCount: Int
  hitsCount: Int
  missesCount: Int
  partialsCount: Int
  branchesCount: Int
  methodsCount: Int
  sessionsCount: Int
  diff: Int

  coverage: Float @deprecated(reason: "Use `percentCovered`")
}
